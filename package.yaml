name: ff

library:
  dependencies:
    - aeson
    - bytestring
    - containers
    - crdt
    - directory
    - filepath
    - text
  source-dirs: lib

executables:
  ff:
    dependencies:
      - bytestring
      - directory
      - filepath
      - yaml

      - ff

    main: Main
    source-dirs: exe

tests:
  test:
    dependencies:
      - containers
      - tasty
      - tasty-hunit

      - ff

    main: Main
    source-dirs: test

dependencies:
  - base
