name: ff-qt

dependencies:
  - base

executable:
  dependencies:
    - crdt
    - hoppy-runtime
    - qtah #-qt5
    - stm
    - text

    - ff-core

  ghc-options: -dynamic -optl-pthread
  main: Main
